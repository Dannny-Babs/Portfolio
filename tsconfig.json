{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": "src/", // This must be specified if "paths" is.
    "paths": {
      "@/*": ["./*"] // All imports starting with "@" will be looked up in the root directory
    }
  }
}